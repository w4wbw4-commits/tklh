// Kept as a stable import path. The implementation now lives in the shared
// domain layer so mobile clients reuse the exact same lead logic.
export { upsertCustomerLead } from "@/domain/leads/service";
