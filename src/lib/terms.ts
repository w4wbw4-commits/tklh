import { usersService } from "@/domain";

export const TERMS_VERSION = usersService.TERMS_VERSION;

export type TermsScope = usersService.TermsScope;

export const recordTermsAcceptance = usersService.recordTermsAcceptance;
